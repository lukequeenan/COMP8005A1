#ifndef THREAD_H
#define THREAD_H

/* System includes */
#include <pthread.h>
#include <stdlib.h>
#include <stdio.h>
#include <sys/time.h>

/* Global prototypes */
int startThreads(long numberOfThreads);

#endif
